- job:
    name: function-test
    display-name: function-test
    scm:
      - git:
          url: https://github.com/qzhuyan/hemir.git
          branches:
            - origin/master
          basedir: hemir
            
    builders:
      - shell: 'docker run -it --rm -u `id -u` -v $WORKSPACE:/workdir --entrypoint=/bin/bash vidiben/casperjs -c casperjs ./hemmirror.js '
